# JavaScript & TypeScript Assessment — Detailed Solutions

See the repository `README.md` for the detailed questions, answers, explanations, code examples, and final answer key for Q2–Q12.

## Covered Questions

- Q2 — JSON serialization and `Object.assign()`
- Q3 — `findIndex()`, `===`, and `splice()`
- Q4 — `Array.flat(5)`
- Q5 — Debouncing vs throttling
- Q6 — Array indexes vs custom properties
- Q7 — Object destructuring and rest operator
- Q8 — Null checking and `NaN`
- Q9 — Error `cause`
- Q10 — `call()`, `bind()`, and `apply()`
- Q11 — `Object.freeze()` and strict mode
- Q12 — TypeScript generics

> Q1 and Q13–Q15 were not included because their screenshots were not available in the conversation at the time this repository was prepared. They should not be guessed.
